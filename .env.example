# Public RPC endpoint embedded in the client bundle at build time.
# NOTE: Any REACT_APP_* value ends up in the shipped JavaScript —
# treat this as a public, rate-limit-scoped identifier, not a secret.
# If you need to protect an RPC key, proxy through a backend instead.
REACT_APP_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
